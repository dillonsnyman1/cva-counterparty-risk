{
  "name": "cva-counterparty-risk",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,ui -c blue,green \"python3 -m uvicorn app.main:app --reload --app-dir backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
